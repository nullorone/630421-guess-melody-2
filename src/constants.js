const Value = {
  EMPTY: 0,
  FULL: 1,
};

const EMPTY_STRING = ``;

const GAME_TIMES = [5, 8, 10];

const AMOUNT_MISTAKES = [1, 3, 5];

const MOCK_ARTISTS = [`Пелагея`, `Краснознаменная дивизия имени моей бабушки`, `Lorde`];

export {
  Value,
  EMPTY_STRING,
  GAME_TIMES,
  AMOUNT_MISTAKES,
  MOCK_ARTISTS
};
